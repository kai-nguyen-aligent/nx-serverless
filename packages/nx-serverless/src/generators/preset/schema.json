{
  "$schema": "https://json-schema.org/schema",
  "$id": "Preset",
  "title": "Nx Generator for bootstrapping Serverless Framework Workspace",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Workspace name"
    },
    "presetVersion": {
      "type": "string",
      "description": "Preset version"
    },
    "nodeVersion": {
      "type": "string",
      "description": "Target Node.js version"
    },
    "packageManager": {
      "type": "string",
      "description": "Target package manager"
    }
  },
  "required": ["name", "presetVersion", "nodeVersion", "packageManager"]
}
